$bucketNames = (aws s3api list-buckets --query "Buckets[].Name" --output text).split("`n")
foreach ($bucket in $bucketNames) {
    aws s3api get-bucket-lifecycle-configuration --bucket $bucket --query "Rules" --output json
}
