If we opt for Lake Formation access controls at the database or table level, our corresponding S3 access points will be set up with a prefix matching the model output or model feature. Alternatively, if Lake Formation access is managed through LF tags, our S3 access points will align with object tags.
